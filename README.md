# salt repo to go with Cloud Bursting

## Setup
- install salt-master
- ```mv master /etc/salt/master```
- ```mv reactor.conf /etc/salt/master.d/```
- follow instructions in cloud bursting repo to not have to manually accept keys
- start salt-master
